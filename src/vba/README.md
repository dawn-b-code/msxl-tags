# src/vba

Future home for exported VBA modules, classes, and forms.

Nothing is implemented yet. When coding starts, prefer text exports here and document any import/export workflow needed to assemble the `.xlam`.
